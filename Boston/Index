
export interface User {
  id: string;
  name: string;
  email: string;
  avatarUrl: string;
  "data-ai-hint"?: string;
  online?: boolean;
}

export interface Group {
  id: string;
  name: string;
  bannerUrl: string;
  "data-ai-hint-banner"?: string;
  members: User[];
  description: string;
}
